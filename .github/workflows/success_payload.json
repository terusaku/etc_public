{
  "blocks": [
    {
      "type": "header",
      "text": {
        "type": "plain_text",
        "text": ":white_check_mark: *Workflow*: <https://github.com/${{ github.repository }}/actions/|${{ github.workflow }}>完了",
        "emoji": true
      }
    },
    {
      "type": "section",
      "fields": [
        {
          "type": "mrkdwn",
          "text": "*Status*: ${{ steps.publish.conclusion }}"
        },
        {
          "type": "mrkdwn",
          "text": "*Commit*: <https://github.com/${{ github.repository }}/commit/${{ github.sha }}|${{ github.sha }}>"
        }
      ]
    }
  ]
}
